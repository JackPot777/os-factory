---
- name: "{{ work_breadcrumb }} - extract built ami id"
  set_fact:
    ami_id: "/subscriptions/{{ azure_subscription_id }}/resourceGroups/{{ azure_ressource_group}}/providers/Microsoft.Compute/images/{{ osf_target_name }}"

- debug:
    msg: "BUILT AMI ID {{ ami_id }}"
    verbosity: 1
