---
- name: "get id of ami"
  ec2_ami_find:
    owner: self
    name: "{{ osf_target_name }}"
    no_result_action: fail
  register: ami_find

- name: "fact for result"
  set_fact:
    ami_id: "{{ ami_find.results[0].ami_id }}"
